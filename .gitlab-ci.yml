image: ruby:2.3.0
before_script:
  - gem install bundler --no-ri --no-rdoc
  - bundle install
test:
  script:
    - bundle exec rake test
rubocop:
  script:
    - bundle exec rubocop --rails --fail-level=warning
